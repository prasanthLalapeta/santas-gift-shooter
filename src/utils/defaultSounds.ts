export const defaultSounds = {
    shoot: 'data:audio/mp3;base64,...', // Add base64 encoded sound
    hit: 'data:audio/mp3;base64,...',   // Add base64 encoded sound
    combo: 'data:audio/mp3;base64,...',  // Add base64 encoded sound
    gameOver: 'data:audio/mp3;base64,...' // Add base64 encoded sound
}; 